import logging
from django.db import transaction
from django.db.models import Q, Count, Avg, Max, Min
from django.core.exceptions import ValidationError
from rest_framework import status
from rest_framework.response import Response
from rest_framework.decorators import action
from rest_framework.viewsets import ModelViewSet

logger = logging.getLogger('api')


class BulkOperationsMixin:
    """Mixin to add bulk operations to ViewSets"""
    
    @action(detail=False, methods=['post'])
    @transaction.atomic
    def bulk_create(self, request):
        """Create multiple objects in a single request"""
        try:
            data_list = request.data.get('objects', [])
            
            if not isinstance(data_list, list):
                return Response({
                    'error': 'Objects must be provided as a list'
                }, status=status.HTTP_400_BAD_REQUEST)
            
            if len(data_list) > 100:
                return Response({
                    'error': 'Maximum 100 objects allowed per bulk operation'
                }, status=status.HTTP_400_BAD_REQUEST)
            
            created_objects = []
            errors = []
            
            for i, data in enumerate(data_list):
                try:
                    serializer = self.get_serializer(data=data)
                    if serializer.is_valid():
                        obj = serializer.save()
                        created_objects.append(serializer.data)
                    else:
                        errors.append({
                            'index': i,
                            'errors': serializer.errors
                        })
                except Exception as e:
                    errors.append({
                        'index': i,
                        'error': str(e)
                    })\n            \n            logger.info(f\"Bulk create: {len(created_objects)} objects created, {len(errors)} errors\")\n            \n            return Response({\n                'created': len(created_objects),\n                'errors': len(errors),\n                'objects': created_objects,\n                'error_details': errors\n            }, status=status.HTTP_201_CREATED if created_objects else status.HTTP_400_BAD_REQUEST)\n            \n        except Exception as e:\n            logger.error(f\"Bulk create failed: {str(e)}\")\n            return Response({\n                'error': 'Bulk create operation failed'\n            }, status=status.HTTP_500_INTERNAL_SERVER_ERROR)\n    \n    @action(detail=False, methods=['patch'])\n    @transaction.atomic\n    def bulk_update(self, request):\n        \"\"\"Update multiple objects in a single request\"\"\"\n        try:\n            updates = request.data.get('updates', [])\n            \n            if not isinstance(updates, list):\n                return Response({\n                    'error': 'Updates must be provided as a list'\n                }, status=status.HTTP_400_BAD_REQUEST)\n            \n            if len(updates) > 50:\n                return Response({\n                    'error': 'Maximum 50 objects allowed per bulk update'\n                }, status=status.HTTP_400_BAD_REQUEST)\n            \n            updated_objects = []\n            errors = []\n            \n            for i, update_data in enumerate(updates):\n                try:\n                    obj_id = update_data.get('id')\n                    if not obj_id:\n                        errors.append({\n                            'index': i,\n                            'error': 'ID is required for updates'\n                        })\n                        continue\n                    \n                    obj = self.get_queryset().get(pk=obj_id)\n                    serializer = self.get_serializer(obj, data=update_data, partial=True)\n                    \n                    if serializer.is_valid():\n                        serializer.save()\n                        updated_objects.append(serializer.data)\n                    else:\n                        errors.append({\n                            'index': i,\n                            'id': obj_id,\n                            'errors': serializer.errors\n                        })\n                        \n                except self.queryset.model.DoesNotExist:\n                    errors.append({\n                        'index': i,\n                        'id': obj_id,\n                        'error': 'Object not found'\n                    })\n                except Exception as e:\n                    errors.append({\n                        'index': i,\n                        'error': str(e)\n                    })\n            \n            logger.info(f\"Bulk update: {len(updated_objects)} objects updated, {len(errors)} errors\")\n            \n            return Response({\n                'updated': len(updated_objects),\n                'errors': len(errors),\n                'objects': updated_objects,\n                'error_details': errors\n            })\n            \n        except Exception as e:\n            logger.error(f\"Bulk update failed: {str(e)}\")\n            return Response({\n                'error': 'Bulk update operation failed'\n            }, status=status.HTTP_500_INTERNAL_SERVER_ERROR)\n    \n    @action(detail=False, methods=['delete'])\n    @transaction.atomic\n    def bulk_delete(self, request):\n        \"\"\"Delete multiple objects in a single request\"\"\"\n        try:\n            ids = request.data.get('ids', [])\n            \n            if not isinstance(ids, list):\n                return Response({\n                    'error': 'IDs must be provided as a list'\n                }, status=status.HTTP_400_BAD_REQUEST)\n            \n            if len(ids) > 50:\n                return Response({\n                    'error': 'Maximum 50 objects allowed per bulk delete'\n                }, status=status.HTTP_400_BAD_REQUEST)\n            \n            deleted_count = 0\n            errors = []\n            \n            for obj_id in ids:\n                try:\n                    obj = self.get_queryset().get(pk=obj_id)\n                    obj.delete()\n                    deleted_count += 1\n                except self.queryset.model.DoesNotExist:\n                    errors.append({\n                        'id': obj_id,\n                        'error': 'Object not found'\n                    })\n                except Exception as e:\n                    errors.append({\n                        'id': obj_id,\n                        'error': str(e)\n                    })\n            \n            logger.info(f\"Bulk delete: {deleted_count} objects deleted, {len(errors)} errors\")\n            \n            return Response({\n                'deleted': deleted_count,\n                'errors': len(errors),\n                'error_details': errors\n            })\n            \n        except Exception as e:\n            logger.error(f\"Bulk delete failed: {str(e)}\")\n            return Response({\n                'error': 'Bulk delete operation failed'\n            }, status=status.HTTP_500_INTERNAL_SERVER_ERROR)\n\n\nclass AdvancedFilteringMixin:\n    \"\"\"Mixin to add advanced filtering capabilities\"\"\"\n    \n    def get_queryset(self):\n        \"\"\"Apply advanced filters to queryset\"\"\"\n        queryset = super().get_queryset()\n        \n        # Apply search filters\n        search = self.request.query_params.get('search')\n        if search and hasattr(self, 'search_fields'):\n            search_query = Q()\n            for field in self.search_fields:\n                search_query |= Q(**{f\"{field}__icontains\": search})\n            queryset = queryset.filter(search_query)\n        \n        # Apply date range filters\n        date_from = self.request.query_params.get('date_from')\n        date_to = self.request.query_params.get('date_to')\n        date_field = getattr(self, 'date_filter_field', 'created_at')\n        \n        if date_from:\n            try:\n                from django.utils.dateparse import parse_datetime\n                parsed_date = parse_datetime(date_from)\n                if parsed_date:\n                    queryset = queryset.filter(**{f\"{date_field}__gte\": parsed_date})\n            except ValueError:\n                pass\n        \n        if date_to:\n            try:\n                from django.utils.dateparse import parse_datetime\n                parsed_date = parse_datetime(date_to)\n                if parsed_date:\n                    queryset = queryset.filter(**{f\"{date_field}__lte\": parsed_date})\n            except ValueError:\n                pass\n        \n        # Apply numeric range filters\n        for param, field in getattr(self, 'numeric_filters', {}).items():\n            min_val = self.request.query_params.get(f\"{param}_min\")\n            max_val = self.request.query_params.get(f\"{param}_max\")\n            \n            if min_val:\n                try:\n                    queryset = queryset.filter(**{f\"{field}__gte\": float(min_val)})\n                except ValueError:\n                    pass\n            \n            if max_val:\n                try:\n                    queryset = queryset.filter(**{f\"{field}__lte\": float(max_val)})\n                except ValueError:\n                    pass\n        \n        # Apply choice filters\n        for param, field in getattr(self, 'choice_filters', {}).items():\n            value = self.request.query_params.get(param)\n            if value:\n                queryset = queryset.filter(**{field: value})\n        \n        # Apply boolean filters\n        for param, field in getattr(self, 'boolean_filters', {}).items():\n            value = self.request.query_params.get(param)\n            if value is not None:\n                bool_val = value.lower() in ['true', '1', 'yes']\n                queryset = queryset.filter(**{field: bool_val})\n        \n        # Apply ordering\n        ordering = self.request.query_params.get('ordering')\n        if ordering and hasattr(self, 'allowed_orderings'):\n            # Remove - prefix for reverse ordering check\n            order_field = ordering.lstrip('-')\n            if order_field in self.allowed_orderings:\n                queryset = queryset.order_by(ordering)\n        \n        return queryset\n    \n    @action(detail=False, methods=['get'])\n    def statistics(self, request):\n        \"\"\"Get statistics for the filtered queryset\"\"\"\n        try:\n            queryset = self.get_queryset()\n            \n            stats = {\n                'total_count': queryset.count(),\n            }\n            \n            # Add numeric field statistics\n            if hasattr(self, 'stats_fields'):\n                for field in self.stats_fields:\n                    field_stats = queryset.aggregate(\n                        **{\n                            f\"{field}_avg\": Avg(field),\n                            f\"{field}_min\": Min(field),\n                            f\"{field}_max\": Max(field),\n                        }\n                    )\n                    stats.update(field_stats)\n            \n            # Add grouping statistics\n            if hasattr(self, 'group_by_fields'):\n                for field in self.group_by_fields:\n                    group_stats = queryset.values(field).annotate(\n                        count=Count('id')\n                    ).order_by('-count')[:10]\n                    stats[f\"{field}_distribution\"] = list(group_stats)\n            \n            return Response(stats)\n            \n        except Exception as e:\n            logger.error(f\"Statistics generation failed: {str(e)}\")\n            return Response({\n                'error': 'Failed to generate statistics'\n            }, status=status.HTTP_500_INTERNAL_SERVER_ERROR)\n\n\nclass AsyncOperationsMixin:\n    \"\"\"Mixin to add async operation support (placeholder for future implementation)\"\"\"\n    \n    @action(detail=False, methods=['post'])\n    def async_export(self, request):\n        \"\"\"Start an async export operation\"\"\"\n        try:\n            # This would typically queue a background task\n            # For now, return a task ID that can be used to check status\n            import uuid\n            task_id = str(uuid.uuid4())\n            \n            logger.info(f\"Async export started with task ID: {task_id}\")\n            \n            return Response({\n                'task_id': task_id,\n                'status': 'started',\n                'message': 'Export task has been queued. Use the task_id to check status.'\n            }, status=status.HTTP_202_ACCEPTED)\n            \n        except Exception as e:\n            logger.error(f\"Async export failed: {str(e)}\")\n            return Response({\n                'error': 'Failed to start export task'\n            }, status=status.HTTP_500_INTERNAL_SERVER_ERROR)\n    \n    @action(detail=False, methods=['get'])\n    def task_status(self, request):\n        \"\"\"Check the status of an async task\"\"\"\n        task_id = request.query_params.get('task_id')\n        \n        if not task_id:\n            return Response({\n                'error': 'task_id parameter is required'\n            }, status=status.HTTP_400_BAD_REQUEST)\n        \n        # This would typically check the task status from a queue/database\n        # For now, return a mock response\n        return Response({\n            'task_id': task_id,\n            'status': 'completed',\n            'progress': 100,\n            'result_url': f'/api/downloads/{task_id}.csv'\n        })
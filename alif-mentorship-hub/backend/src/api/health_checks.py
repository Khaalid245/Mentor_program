import logging\nimport time\nfrom typing import Dict, List, Any, Optional\nfrom django.http import JsonResponse\nfrom django.views import View\nfrom django.db import connection\nfrom django.core.cache import cache\nfrom django.conf import settings\nfrom django.utils.decorators import method_decorator\nfrom django.views.decorators.csrf import csrf_exempt\nfrom django.views.decorators.cache import never_cache\nimport redis\nimport requests\nfrom datetime import datetime, timedelta\n\nlogger = logging.getLogger('api.health')\n\n\nclass HealthCheckStatus:\n    \"\"\"Health check status constants\"\"\"\n    HEALTHY = 'healthy'\n    UNHEALTHY = 'unhealthy'\n    DEGRADED = 'degraded'\n    UNKNOWN = 'unknown'\n\n\nclass BaseHealthCheck:\n    \"\"\"Base class for health checks\"\"\"\n    \n    def __init__(self, name: str, timeout: int = 5):\n        self.name = name\n        self.timeout = timeout\n    \n    def check(self) -> Dict[str, Any]:\n        \"\"\"Perform health check and return status\"\"\"\n        start_time = time.time()\n        \n        try:\n            result = self._perform_check()\n            duration = time.time() - start_time\n            \n            return {\n                'name': self.name,\n                'status': result.get('status', HealthCheckStatus.HEALTHY),\n                'duration_ms': round(duration * 1000, 2),\n                'timestamp': datetime.utcnow().isoformat(),\n                'details': result.get('details', {}),\n                'message': result.get('message', 'OK')\n            }\n        except Exception as e:\n            duration = time.time() - start_time\n            logger.error(f\"Health check '{self.name}' failed: {str(e)}\")\n            \n            return {\n                'name': self.name,\n                'status': HealthCheckStatus.UNHEALTHY,\n                'duration_ms': round(duration * 1000, 2),\n                'timestamp': datetime.utcnow().isoformat(),\n                'details': {'error': str(e)},\n                'message': f'Health check failed: {str(e)}'\n            }\n    \n    def _perform_check(self) -> Dict[str, Any]:\n        \"\"\"Override this method to implement specific health check logic\"\"\"\n        raise NotImplementedError\n\n\nclass DatabaseHealthCheck(BaseHealthCheck):\n    \"\"\"Check database connectivity and performance\"\"\"\n    \n    def __init__(self, timeout: int = 5):\n        super().__init__('database', timeout)\n    \n    def _perform_check(self) -> Dict[str, Any]:\n        start_time = time.time()\n        \n        # Test basic connectivity\n        with connection.cursor() as cursor:\n            cursor.execute(\"SELECT 1\")\n            result = cursor.fetchone()\n        \n        if result[0] != 1:\n            return {\n                'status': HealthCheckStatus.UNHEALTHY,\n                'message': 'Database query returned unexpected result'\n            }\n        \n        query_time = time.time() - start_time\n        \n        # Check connection pool status\n        pool_info = self._get_connection_pool_info()\n        \n        # Determine status based on query time\n        if query_time > 1.0:  # 1 second threshold\n            status = HealthCheckStatus.DEGRADED\n            message = f'Database responding slowly ({query_time:.2f}s)'\n        else:\n            status = HealthCheckStatus.HEALTHY\n            message = 'Database is healthy'\n        \n        return {\n            'status': status,\n            'message': message,\n            'details': {\n                'query_time_ms': round(query_time * 1000, 2),\n                'connection_pool': pool_info\n            }\n        }\n    \n    def _get_connection_pool_info(self) -> Dict[str, Any]:\n        \"\"\"Get connection pool information\"\"\"\n        try:\n            # This is database-specific, adjust for your database\n            with connection.cursor() as cursor:\n                cursor.execute(\"SHOW STATUS LIKE 'Threads_connected'\")\n                connected = cursor.fetchone()\n                cursor.execute(\"SHOW STATUS LIKE 'Max_used_connections'\")\n                max_used = cursor.fetchone()\n                \n                return {\n                    'threads_connected': connected[1] if connected else 'unknown',\n                    'max_used_connections': max_used[1] if max_used else 'unknown'\n                }\n        except Exception as e:\n            return {'error': str(e)}\n\n\nclass CacheHealthCheck(BaseHealthCheck):\n    \"\"\"Check cache (Redis) connectivity and performance\"\"\"\n    \n    def __init__(self, timeout: int = 5):\n        super().__init__('cache', timeout)\n    \n    def _perform_check(self) -> Dict[str, Any]:\n        test_key = 'health_check_test'\n        test_value = f'test_{int(time.time())}'\n        \n        start_time = time.time()\n        \n        # Test set operation\n        cache.set(test_key, test_value, timeout=60)\n        \n        # Test get operation\n        retrieved_value = cache.get(test_key)\n        \n        # Test delete operation\n        cache.delete(test_key)\n        \n        operation_time = time.time() - start_time\n        \n        if retrieved_value != test_value:\n            return {\n                'status': HealthCheckStatus.UNHEALTHY,\n                'message': 'Cache set/get operation failed'\n            }\n        \n        # Get Redis info if using Redis\n        redis_info = self._get_redis_info()\n        \n        # Determine status based on operation time\n        if operation_time > 0.5:  # 500ms threshold\n            status = HealthCheckStatus.DEGRADED\n            message = f'Cache responding slowly ({operation_time:.2f}s)'\n        else:\n            status = HealthCheckStatus.HEALTHY\n            message = 'Cache is healthy'\n        \n        return {\n            'status': status,\n            'message': message,\n            'details': {\n                'operation_time_ms': round(operation_time * 1000, 2),\n                'redis_info': redis_info\n            }\n        }\n    \n    def _get_redis_info(self) -> Dict[str, Any]:\n        \"\"\"Get Redis server information\"\"\"\n        try:\n            # Try to get Redis connection info\n            from django_redis import get_redis_connection\n            redis_conn = get_redis_connection(\"default\")\n            info = redis_conn.info()\n            \n            return {\n                'redis_version': info.get('redis_version'),\n                'connected_clients': info.get('connected_clients'),\n                'used_memory_human': info.get('used_memory_human'),\n                'uptime_in_seconds': info.get('uptime_in_seconds')\n            }\n        except Exception as e:\n            return {'error': str(e)}\n\n\nclass ExternalServiceHealthCheck(BaseHealthCheck):\n    \"\"\"Check external service connectivity\"\"\"\n    \n    def __init__(self, name: str, url: str, timeout: int = 10):\n        super().__init__(f'external_service_{name}', timeout)\n        self.url = url\n        self.service_name = name\n    \n    def _perform_check(self) -> Dict[str, Any]:\n        start_time = time.time()\n        \n        try:\n            response = requests.get(self.url, timeout=self.timeout)\n            response_time = time.time() - start_time\n            \n            if response.status_code == 200:\n                status = HealthCheckStatus.HEALTHY\n                message = f'{self.service_name} is accessible'\n            else:\n                status = HealthCheckStatus.DEGRADED\n                message = f'{self.service_name} returned status {response.status_code}'\n            \n            return {\n                'status': status,\n                'message': message,\n                'details': {\n                    'url': self.url,\n                    'status_code': response.status_code,\n                    'response_time_ms': round(response_time * 1000, 2)\n                }\n            }\n        \n        except requests.exceptions.Timeout:\n            return {\n                'status': HealthCheckStatus.UNHEALTHY,\n                'message': f'{self.service_name} request timed out',\n                'details': {'url': self.url, 'timeout': self.timeout}\n            }\n        \n        except requests.exceptions.ConnectionError:\n            return {\n                'status': HealthCheckStatus.UNHEALTHY,\n                'message': f'{self.service_name} connection failed',\n                'details': {'url': self.url}\n            }\n\n\nclass DiskSpaceHealthCheck(BaseHealthCheck):\n    \"\"\"Check available disk space\"\"\"\n    \n    def __init__(self, path: str = '/', warning_threshold: float = 0.8, critical_threshold: float = 0.9):\n        super().__init__('disk_space')\n        self.path = path\n        self.warning_threshold = warning_threshold\n        self.critical_threshold = critical_threshold\n    \n    def _perform_check(self) -> Dict[str, Any]:\n        import shutil\n        \n        total, used, free = shutil.disk_usage(self.path)\n        usage_percent = used / total\n        \n        if usage_percent >= self.critical_threshold:\n            status = HealthCheckStatus.UNHEALTHY\n            message = f'Disk usage critical: {usage_percent:.1%}'\n        elif usage_percent >= self.warning_threshold:\n            status = HealthCheckStatus.DEGRADED\n            message = f'Disk usage high: {usage_percent:.1%}'\n        else:\n            status = HealthCheckStatus.HEALTHY\n            message = f'Disk usage normal: {usage_percent:.1%}'\n        \n        return {\n            'status': status,\n            'message': message,\n            'details': {\n                'path': self.path,\n                'total_gb': round(total / (1024**3), 2),\n                'used_gb': round(used / (1024**3), 2),\n                'free_gb': round(free / (1024**3), 2),\n                'usage_percent': round(usage_percent * 100, 1)\n            }\n        }\n\n\nclass MemoryHealthCheck(BaseHealthCheck):\n    \"\"\"Check memory usage\"\"\"\n    \n    def __init__(self, warning_threshold: float = 0.8, critical_threshold: float = 0.9):\n        super().__init__('memory')\n        self.warning_threshold = warning_threshold\n        self.critical_threshold = critical_threshold\n    \n    def _perform_check(self) -> Dict[str, Any]:\n        import psutil\n        \n        memory = psutil.virtual_memory()\n        usage_percent = memory.percent / 100\n        \n        if usage_percent >= self.critical_threshold:\n            status = HealthCheckStatus.UNHEALTHY\n            message = f'Memory usage critical: {usage_percent:.1%}'\n        elif usage_percent >= self.warning_threshold:\n            status = HealthCheckStatus.DEGRADED\n            message = f'Memory usage high: {usage_percent:.1%}'\n        else:\n            status = HealthCheckStatus.HEALTHY\n            message = f'Memory usage normal: {usage_percent:.1%}'\n        \n        return {\n            'status': status,\n            'message': message,\n            'details': {\n                'total_gb': round(memory.total / (1024**3), 2),\n                'available_gb': round(memory.available / (1024**3), 2),\n                'used_gb': round(memory.used / (1024**3), 2),\n                'usage_percent': memory.percent\n            }\n        }\n\n\nclass HealthCheckManager:\n    \"\"\"Manages and executes health checks\"\"\"\n    \n    def __init__(self):\n        self.checks: List[BaseHealthCheck] = []\n        self._register_default_checks()\n    \n    def _register_default_checks(self):\n        \"\"\"Register default health checks\"\"\"\n        # Database check\n        self.register_check(DatabaseHealthCheck())\n        \n        # Cache check (if Redis is configured)\n        if hasattr(settings, 'CACHES') and 'default' in settings.CACHES:\n            self.register_check(CacheHealthCheck())\n        \n        # System resource checks\n        self.register_check(DiskSpaceHealthCheck())\n        \n        try:\n            import psutil\n            self.register_check(MemoryHealthCheck())\n        except ImportError:\n            logger.warning(\"psutil not installed, skipping memory health check\")\n        \n        # External service checks (configure as needed)\n        # self.register_check(ExternalServiceHealthCheck('api_gateway', 'https://api.example.com/health'))\n    \n    def register_check(self, check: BaseHealthCheck):\n        \"\"\"Register a health check\"\"\"\n        self.checks.append(check)\n        logger.info(f\"Registered health check: {check.name}\")\n    \n    def run_checks(self, check_names: Optional[List[str]] = None) -> Dict[str, Any]:\n        \"\"\"Run health checks and return results\"\"\"\n        start_time = time.time()\n        \n        # Filter checks if specific names provided\n        checks_to_run = self.checks\n        if check_names:\n            checks_to_run = [check for check in self.checks if check.name in check_names]\n        \n        results = []\n        overall_status = HealthCheckStatus.HEALTHY\n        \n        for check in checks_to_run:\n            result = check.check()\n            results.append(result)\n            \n            # Determine overall status\n            if result['status'] == HealthCheckStatus.UNHEALTHY:\n                overall_status = HealthCheckStatus.UNHEALTHY\n            elif result['status'] == HealthCheckStatus.DEGRADED and overall_status == HealthCheckStatus.HEALTHY:\n                overall_status = HealthCheckStatus.DEGRADED\n        \n        total_duration = time.time() - start_time\n        \n        return {\n            'status': overall_status,\n            'timestamp': datetime.utcnow().isoformat(),\n            'duration_ms': round(total_duration * 1000, 2),\n            'checks': results,\n            'summary': {\n                'total_checks': len(results),\n                'healthy': len([r for r in results if r['status'] == HealthCheckStatus.HEALTHY]),\n                'degraded': len([r for r in results if r['status'] == HealthCheckStatus.DEGRADED]),\n                'unhealthy': len([r for r in results if r['status'] == HealthCheckStatus.UNHEALTHY])\n            }\n        }\n\n\n# Global health check manager\nhealth_manager = HealthCheckManager()\n\n\n@method_decorator([csrf_exempt, never_cache], name='dispatch')\nclass HealthCheckView(View):\n    \"\"\"Health check endpoint for monitoring systems\"\"\"\n    \n    def get(self, request):\n        \"\"\"Perform health checks and return status\"\"\"\n        # Get specific checks to run (optional)\n        check_names = request.GET.getlist('check')\n        \n        # Run health checks\n        results = health_manager.run_checks(check_names if check_names else None)\n        \n        # Determine HTTP status code\n        if results['status'] == HealthCheckStatus.HEALTHY:\n            status_code = 200\n        elif results['status'] == HealthCheckStatus.DEGRADED:\n            status_code = 200  # Still operational\n        else:\n            status_code = 503  # Service unavailable\n        \n        # Log health check results\n        logger.info(f\"Health check completed: {results['status']} ({results['duration_ms']}ms)\")\n        \n        return JsonResponse(results, status=status_code)\n\n\n@method_decorator([csrf_exempt, never_cache], name='dispatch')\nclass ReadinessCheckView(View):\n    \"\"\"Readiness check for Kubernetes/container orchestration\"\"\"\n    \n    def get(self, request):\n        \"\"\"Check if application is ready to serve traffic\"\"\"\n        # Run critical checks only (database, cache)\n        critical_checks = ['database', 'cache']\n        results = health_manager.run_checks(critical_checks)\n        \n        # For readiness, we're more strict - any unhealthy check fails readiness\n        if results['status'] in [HealthCheckStatus.HEALTHY, HealthCheckStatus.DEGRADED]:\n            status_code = 200\n            ready = True\n        else:\n            status_code = 503\n            ready = False\n        \n        response_data = {\n            'ready': ready,\n            'status': results['status'],\n            'timestamp': results['timestamp'],\n            'checks': results['checks']\n        }\n        \n        return JsonResponse(response_data, status=status_code)\n\n\n@method_decorator([csrf_exempt, never_cache], name='dispatch')\nclass LivenessCheckView(View):\n    \"\"\"Liveness check for Kubernetes/container orchestration\"\"\"\n    \n    def get(self, request):\n        \"\"\"Check if application is alive (basic functionality)\"\"\"\n        # Simple check - if we can respond, we're alive\n        response_data = {\n            'alive': True,\n            'timestamp': datetime.utcnow().isoformat(),\n            'version': getattr(settings, 'VERSION', 'unknown')\n        }\n        \n        return JsonResponse(response_data, status=200)